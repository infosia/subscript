// Pull the three strictly-narrower R23 rejects into the standing stock-tsc
// project without making the language compiler treat reject entries as accepts.
import "../reject/r121-wire-enum-fractional";
import "../reject/r122-wire-enum-duplicate";
import "../reject/r123-wire-enum-out-of-range";
